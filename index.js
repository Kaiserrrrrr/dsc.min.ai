require("express")().get("/",function(req, res){res.send("")}).listen(0);const{Client,GatewayIntentBits}=require("discord.js");const f=require("node-fetch");const c=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildMessages,GatewayIntentBits.MessageContent]});c.on("ready",()=>{console.log(`${c.user.tag} logged in!`);});c.on("messageCreate",async(m)=>{if(m.mentions.users.has(c.user.id) && !m.author.bot){const r=await f(`${process.env.apiUrl}uid=${m.author.id}&msg=${encodeURIComponent(m.content)}`);const d =await r.json();m.reply(d.cnt)}});c.login(process.env.token);
